<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>OAuth Demo</title>
        <style>
            body{
                font-family: arial;
                margin: 0;
            }
            nav{
                background: #5ca8f0;
                padding: 10px;
            }
            nav ul{
                max-width: 960px;
                margin: 0 auto;
                padding: 0;
            }
            nav li{
                list-style-type: none;
                display: inline-block;
                margin: 0 10px 0 0;
            }
            nav a{
                color: #fff;
                font-size: 18px;
                text-decoration: none;
                padding: 10px;
                display: block;
            }
            main, header{
                max-width: 960px;
                margin: 30px auto;
                padding: 0 10px;
                color: #333;
            }

            img{
                height: 200px;
                width: 200px;
            }
        </style>
    </head>
    <body>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/auth/logout">Logout</a></li>
            </ul>
        </nav>
        <header>
            <h1>Welcome <%= user.username %></h1>           
        </header>
        <main>
            <h3>logged in via <%= user.socialNetwork %></h3>  
            <img alt="profile" src=<%= user.profileImage %>>
        </main>
    </body>
</html>